javascript:(function() {  var links = document.getElementsByTagName('a');  var domainSet = new Set();    for (var i = 0; i < links.length; i++) {    var href = links[i].href;    var mediaurlIndex = href.indexOf('mediaurl=');        if (mediaurlIndex !== -1) {      var mediaurlSubstring = href.slice(mediaurlIndex + 'mediaurl='.length);      var cdnurlIndex = mediaurlSubstring.indexOf('&cdnurl=');            if (cdnurlIndex !== -1) {        mediaurlSubstring = mediaurlSubstring.slice(0, cdnurlIndex);                try {          var url = new URL(decodeURIComponent(mediaurlSubstring));          domainSet.add(url.hostname);        } catch (error) {          console.error('Error al analizar la URL: ' + mediaurlSubstring);        }      }    }  }  if (domainSet.size === 0) {    alert('No se encontraron URLs con "mediaurl" en su interior.');  } else {    var domainList = Array.from(domainSet).join('\n');        var today = new Date();    var year = today.getFullYear();    var month = (today.getMonth() + 1).toString().padStart(2, '0');    var day = today.getDate().toString().padStart(2, '0');    var filename = year + '-' + month + '-' + day + ' Dominios Bing imágenes.txt';        var blob = new Blob([domainList], { type: 'text/plain' });    var url = URL.createObjectURL(blob);        var a = document.createElement('a');    a.style.display = 'none';    a.href = url;    a.download = filename;    document.body.appendChild(a);    a.click();    window.URL.revokeObjectURL(url);  }})();